# Review branch note

This branch is an additive currentization overlay for `PauseBeforeHarmProtocol/the-record`.

Merge without deleting the preserved legacy archive:

- `the-record.html`
- `entries_array.js`
- `THE-RECORD-COMPLETE.pdf`
- `docs/`

The new root `index.html` is a navigable July 19 review-candidate front door. `weekly/` restores the seven-day view, `agencies/` is now a top-level route, and `institutions/` remains as a compatibility path. Historical source totals remain intentionally unresolved until the repository computes one authoritative count during build and CI.

The five legacy Claude Opus 4 AI Opinion documents retain their supplied authorship and bytes. Current maintenance credits are documented separately in `AI_PROVENANCE.md`.
