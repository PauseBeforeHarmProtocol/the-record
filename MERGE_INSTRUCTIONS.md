# Merge instructions

1. Revoke the personal access token exposed in chat before doing any repository work.
2. Clone `PauseBeforeHarmProtocol/the-record` through an authenticated GitHub session.
3. Create `agent/record-july-18-release` from `main`.
4. Copy the contents of `site/` into the repository root.
5. Preserve the existing `the-record.html`, `entries_array.js`, `THE-RECORD-COMPLETE.pdf`, and `docs/` paths.
6. Run `python scripts/validate.py` after copying `scripts/validate.py` into the repository.
7. Review the new root page and both current sections on desktop and phone.
8. Commit with: `Update The Record through July 18, 2026`.
9. Push the branch and open a draft pull request.

Checked source window: 2026-07-18 12:00 PM EDT.
